import Logger from '../../src/js/utils/logger';

describe('Message Test', () => {
    var logger;
    
    beforeEach(() => {
        logger = new Logger();
    });
    
    it('should log info', () => {

    });
});